/**
 * Liquid glass.
 *
 * `LiquidGlass` is a port of liquid-glass-react (`.ui/liquid-glass-react`,
 * MIT) — see `LiquidGlass.tsx` for the adaptations that let it act as a
 * whole-application material. `surface.tsx` binds it to the user's tuning and
 * exposes the composition helpers the app uses.
 */
export * from './surface'
